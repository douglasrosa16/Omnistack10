import React from 'react'; //Preciso importar quando tiver HTML na pagina
import ReactDOM from 'react-dom'; //DOM = Árvore de Elementos
import App from './App'; 

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (JavaScript + HTML) por exemplo a tag <App >